{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        <div class="col-md-4 col-md-offset-4">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success" type="submit" formnovalidate>Put on the market</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

